<div
    class="modal fade"
    tabindex="-1"
    aria-hidden="true"
    data-calendar-target="modal"
    id="calendarModal"
>
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 data-title class="modal-title">title</h5>
                <button type="button" data-close class="btn-close"
                        data-bs-dismiss="modal"
                        aria-label="Close"></button>
            </div>
            <div class="modal-body" data-content>
                ...
            </div>
            <div class="modal-footer">
                <button type="button" data-close class="btn btn-secondary"
                        data-bs-dismiss="modal">Close
                </button>
                <button type="button" class="btn btn-primary" data-action="action">
                    Confirm
                </button>


            </div>
        </div>
    </div>
</div>
